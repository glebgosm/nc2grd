nc2grd is a console utility converting NetCDF files to Golden Surfer .grd format.
Additionaly, timeInfo is an auxiliary tool to extract time data from .nc files and store it in a temp .txt file.

Created in Visual Studio 2010

For usage details see nc2grd_manual.doc
